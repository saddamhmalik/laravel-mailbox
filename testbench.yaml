laravel: '@testbench'

providers:
  - LaravelMailbox\Providers\MailboxServiceProvider
  - Livewire\LivewireServiceProvider

workbench:
  start: '/mailbox'
  install: false
  health: false
  discovers:
    web: true
    config: true

env:
  - TESTBENCH_WITHOUT_DEFAULT_MIGRATIONS=true
  - APP_ENV=local
  - MAIL_MAILER=mailbox
  - MAILBOX_LOCAL_ONLY=false
  - MAILBOX_STORAGE_DRIVER=database
